name: CI

on:
  push:
    branches:
      - main
  pull_request:

# Default to read-only; the deploy job opts into id-token for OIDC.
permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.4.2'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Check for unused files/exports/dependencies
        run: bun run knip

      - name: Build static files
        run: bun run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          retention-days: 1

  deploy:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::322859817636:role/github-actions-deploy
          aws-region: us-west-2

      - name: Deploy static files to S3 bucket
        run: |
          # Root files (index.html, favicon.svg, resume.pdf) are not
          # fingerprinted — must revalidate so new deploys are picked up.
          # assets/* is excluded here so this pass never touches or deletes it.
          # trakt.json and weather.json are written independently by the
          # scheduled poller Lambdas; song.json and album.json are written
          # independently by a local script (see scripts/song-of-the-month/).
          # All four must survive site deploys.
          aws s3 sync ./dist/ s3://${{ secrets.AWS_S3_BUCKET }} --delete \
            --exclude "assets/*" \
            --exclude "trakt.json" \
            --exclude "weather.json" \
            --exclude "song.json" \
            --exclude "album.json" \
            --cache-control "no-cache"
          # Vite fingerprints everything under assets/ — safe to cache forever.
          aws s3 sync ./dist/assets/ s3://${{ secrets.AWS_S3_BUCKET }}/assets/ --delete \
            --cache-control "public, max-age=31536000, immutable"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.AWS_CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
